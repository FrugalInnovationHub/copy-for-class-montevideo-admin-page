rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public access for admin panel
    // No authentication or App Check required
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

